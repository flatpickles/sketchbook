import Project from '$lib/base/Project/Project';

export default class EmptyProject extends Project {
    // empty = false;
    // button = 'Click me';
    // dob = 'bonk';
    // not so empty now!!
}
